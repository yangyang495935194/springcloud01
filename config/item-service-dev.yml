# html xml
# yaml yet another markup language 腋毛

# 向注册中心注册的服务名
spring:
  application:
    name: item-service
  cloud:
    config:
      override-none: true

server:
  port: 8001
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka
